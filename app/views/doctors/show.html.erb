<h1><%= @doctor.name %></h1>
<p>
<%= @doctor.appointments.map do |doc| %>
  <%= link_to doc.patient.name, patient_path(@doctor.patient_ids)%> - <%= doc.appointment_datetime.strftime("%B %d, %Y at %k:%M") %><br>
%>
<% end %>
</p>
